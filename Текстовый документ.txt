	/*
		db, err := sql.Open("sqlite3", "scheduler.db")
		if err != nil {
			fmt.Println("err:", err)
		}
		defer db.Close()

			_, err = db.Exec("create table if not exists scheduler (id INTEGER PRIMARY KEY AUTOINCREMENT, date VARCHAR, title VARCHAR, comment VARCHAR,  repeat VARCHAR(128))")
			if err != nil {
				fmt.Println("err:", err)
			}
			fmt.Println("Table created successfully")

		_, err = db.Exec("create index if not exists idx_date on scheduler(id)")
		if err != nil {
			fmt.Println("err:", err)
		}
		fmt.Println("Table and index created successfully")
	*/